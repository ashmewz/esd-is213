{
  "permissions": {
    "allow": [
      "Bash(docker compose run --rm --no-deps --env-file .env seat-allocation-service python -c ':*)",
      "Bash(docker port:*)",
      "Bash(curl -s http://localhost:8000/events)",
      "Bash(python -c \"import sys,json; data=json.load\\(sys.stdin\\); print\\(f'OK — {len\\(data\\)} events'\\)\")"
    ]
  }
}
